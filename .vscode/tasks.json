{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Open Claude Design: Build release",
      "type": "process",
      "command": "bash",
      "args": ["scripts/build-release.sh"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Build the wheel, source archive, installer assets, and checksums."
    }
  ]
}
